# Discord webhook URL for alerts (create at Server Settings → Integrations → Webhooks)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/CHANGE_ME

# Tailscale auth key (reusable + ephemeral recommended)
TS_AUTHKEY=tskey-auth-CHANGE_ME
